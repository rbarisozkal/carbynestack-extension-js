# Chrome Extension Communication Package

A package for simplifying communication between Chrome extensions and web apps.

## Installation

Install the package using npm:

```bash
npm install chrome-extension-js
```
